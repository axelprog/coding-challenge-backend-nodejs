/* eslint-disable arrow-body-style */
// const controller = require('./bikes.controller');

describe('Test bikes', () => {
  beforeEach(() => {});

  afterEach(() => {});

  test('TODO: should do unit test for bikes', () => {
  });
});
